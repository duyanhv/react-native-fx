import type { NativeFxGroupProps } from './FxGroupView';

/**
 * Renders nothing on web because fx native presentation surfaces are mobile-only.
 */
export function FxGroupView(_props: NativeFxGroupProps): null {
  return null;
}
