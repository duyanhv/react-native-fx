// Public API — the only stability contract. The component vocabulary (`fx`,
// `FxPresence`, `FxView`, `FxPressable`, `Fx`, `FxGroup`, `FxItem`) is added as
// each component is built; for now this exposes the effect vocabulary types only.
export type { ShaderId } from './effects/catalog';
