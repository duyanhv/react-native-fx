Unverified claims:

- `aurora`, `noise-field`, `plasma`, `caustics`, and `edge-glow` are ratified catalog ids, but they do not render yet. U3-006 owns native MSL+AGSL implementation and package exposure.

Changes:

- Created the DOC-007 task spec with authority links, lifecycle, proof, and scope guards.
- Updated `20` to ratify full-grid mesh controls, reject V1 N-stop auto-placement sugar, and keep `drift` mesh-only.
- Updated `22` to ratify the 10-id shader catalog and shared minimal V1 public uniform scope.
- Updated `50` to record the V1 shader catalog and implementation-status split.
- Updated `data-layer.md` to remove candidate wording for FX-004 and align shader uniform wording.
- Marked FX-001 and FX-004 resolved in `decision-ledger.md`.
- Moved DOC-007 to `merged` and added U3-006 as the follow-up implementation task in `progress.md`.
- Reviewed and approved. Watch-items remain for U3-006 (`V1 catalog` vs shipped ids) and DOC-004 (`edge-glow` shader id vs optional component sugar).

Next: Start DOC-008 to unblock U3-001 symbol scope.
