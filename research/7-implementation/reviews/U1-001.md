# U1-001 · package scaffolding — review

Date: 2026-06-07

## Verdict

Approved. The implementation is correct and follows all guide conventions. Two minor TSDoc issues identified and fixed — internal research doc references removed from public-facing docblocks.

## Checklist

- [x] spec'd — task README is comprehensive, authority links are explicit
- [x] rules-gated — no CLAUDE.md rule violated
- [x] implemented — all 5 work items completed (package.json, expo-module.config.json, ios, android, src)
- [x] commented — comments follow the iceberg principle; guides adhered
- [x] headless-done — `bun run build`, `tsc --noEmit`, `bun run lint`, `bun run swift:lint` all pass
- [x] reviewed
- [ ] docs-closed — SHIP-001 true in 52; IMPL-001 true in architecture/data-layer
- [ ] merged

## Verification

| Gate | Command / Check | Result |
|------|-----------------|--------|
| Build | `bun run build` | Pass |
| TypeScript | `./node_modules/.bin/tsc --noEmit` | Pass |
| Lint | `bun run lint` (Biome) | Pass (9 files) |
| Swift lint | `bun run swift:lint` | Pass |

## Issues Found

### Fixed

1. **`src/runtime/FxEffectView.tsx:7`** — Removed internal reference `(research/4-runtime/30)` from public TSDoc. Replaced with self-contained description.
2. **`src/runtime/FxEffectView.tsx:7`** — Removed period from single-phrase TSDoc description per Code Comments Guide.
3. **`src/effects/catalog.ts:3`** — Removed internal reference `(data-layer §6)` from public TSDoc.

### Accepted

- Placeholder file comments (`manifest/types.ts`, `surface/types.ts`, `motion/types.ts`, `presets/types.ts`) with internal references retained — these are internal scaffolding comments for contributors, not public API surface.
- Native code comments (`FxModule.swift`, `FxModule.kt`) with `data-layer §9 D2` references retained — internal contributor comments.

## Naming

- `Fx` prefix on all native classes (`FxModule`, `FxSurfaceView`, `FxUniforms`, `FxEffectView`)
- Full words over abbreviations (exceptions: established platform terms `MTKView`, `AGSL`, `fbm`)
- Verb-led method names (`setUpMetal`, `loadShaderLibrary`, `applyResolvedConfig`)
- Platform encoding in file names (`FxEffectView.web.tsx`)

## Comments

- Iceberg principle followed throughout — comments document constraints, platform quirks, threading, and cross-references
- `// MARK:` sections used correctly in Swift
- Third-person declarative style in all docblocks
- No commented-out code or stale comments

## Next

Complete the `docs-closed` gate: SHIP-001 must be true in `52`; IMPL-001 must be true in `architecture`/`data-layer`.
