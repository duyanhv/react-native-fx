# U6-002 · the hard-retarget matrix — review

Date: 2026-06-12
Branch: `integration/0.1.x` · device-verify task (no shipped code), evidence reviewed
post-run, maintainer-ratified 2026-06-12
Scope: the matrix evidence (`tasks/U6-002/evidence/matrix.md` + raw logs) against the task
contract (`tasks/U6-002/README.md`, spec'd by the planner 2026-06-12) and the RT-016 close
condition. The planner thread wrote this document; the run was an agent-device session, the
PASS ratification the maintainer's.

## Verdict

**Approved; RT-016 closed.** All nine rows × two platforms PASS with genuine log evidence;
the planner spot-checked the write-up's load-bearing lines against the raw captures and
they reconcile exactly. The integrator-flip trigger did not fire — the DOC-009 disposition
(iOS render-server-first / integrator-on-retarget; Android stock
`animateToFinalPosition()`) is now device truth, not design-time judgment. No follow-up
rework is scoped. `reviewed` is satisfied by this document; `merged` remains the
maintainer's.

## Evidence reconciliation (planner spot-checks against the raw logs)

- **Extension carries** — `carriedVelocity …translationX=151.245` present verbatim in
  `ios-raw.log`; the reversal row's all-zero clip line likewise. A clip firing on
  extension was the named defect to watch for; it does not.
- **Per-channel clip in one line** — the mixed-channel row's
  `carriedVelocity=opacity=0.000 scale=-0.076 translationX=151.310` confirms the clip is
  per-axis, not envelope-global: opacity (reversed) clipped, translation (extended)
  carried, simultaneously.
- **Rapid-fire provenance** — ≥2 retargets log `source=display-link` inside one envelope;
  exactly one completion follows, settled at the last target. This extends the U6-001
  second-retarget regression check to a three-retarget burst.
- **After-rest boundary** — the post-settle second envelope logs `source=render-server`
  (iOS) / `retarget=false` (Android): a fresh fire-once, not a stale display-link
  continuation.
- **Rotation + combined channels** — the U6-001 gap (every prior vector ran rotation=0)
  is closed: presentation decomposition mid-flight under scale+rotation+translation reads
  consistent values (`scale=0.866 tx=107.337 rotation=0.447`) with no skew or jump on the
  recording. Platform-native units held (radians vs degrees).
- **JS-thread silence** — 11 trigger lines per platform (one per scenario, matching the
  9 rows: timing×3, reversal/extension×2, six singletons) and zero per-frame JS; every
  motion line is native. Rule #1 holds under instrumentation designed to prove it (the
  scenario runner schedules retargets on native timers after a single JS trigger).
- **Cancel teardown** — iOS `displayLinkWasActive=true` torn down with no post-cancel
  completion; Android `activeBefore=4 activeAfter=0`.

## Process notes

- **The calibration rerun is correct method, disclosed honestly:** the first Android pass
  used iOS-tuned 300 ms offsets against the faster default `SpringForce` (~0.26 s settle),
  so its "retargets" fired post-settle and proved nothing about mid-flight behavior. The
  agent caught this from the `retarget=false` provenance, recalibrated to 120/200 ms, and
  reran. The platform-speed difference itself is the shape-native law at work, not a
  defect; the catalog feel work stays with U6-003/MOT-001.
- **Cleanup verified by the reviewer:** remnant grep for the instrumentation symbols
  (`U6-002`, `runDebugScenario`, `fxDebugScenario`, `debugCurrentValue`) across
  `packages/` and `example/` returns nothing; `git diff --check` clean; the only tree
  changes are evidence, notes, and tracker ticks.
- **One harmless instruction deviation:** the spec said leave progress state alone; the
  agent flipped U6-002 to `device-pending`, which is the protocol-correct resting state
  for a completed-but-unratified device task. Accepted.
- iOS evidence is simulator (precedent: U5-001/U6-001); the physical-iPhone re-run stays
  on the standing-gates list.

## Closure

- **RT-016 → resolved** in `decision-ledger.md` (close condition genuinely true: on-device
  proof of the retarget cases, both platforms).
- **`34`**: status line advanced (matrix device-verified); the animator-sufficiency open
  question annotated device-proven.
- **`structure.{ios,android}.md` §motion**: confirmed on device, no divergence — no pin
  edit (per the spec's docs-closed rule, a pin changes only when evidence contradicts it).
- **Not closed:** MOT-001/MOT-002 (catalog values and tune stay device-pending —
  U6-003/U7-002), REAL-001 (the M3 floor — U6-003).
