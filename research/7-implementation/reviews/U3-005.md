# U3-005 · shader asset packaging + runtime load proof — review

Date: 2026-06-09
Branch: `integration/0.1.x`
Scope: implementation vs design (`research/7-implementation` + the platform-mechanic contract
`research/5-realization/structure.{ios,android}.md` + the publishing doc `6-ship/52`). Review-only —
the reviewer changed no code or docs.

## Verdict

**Approved**, contingent on one trivial doc-consistency fix (finding ①/②, a two-line reconciliation in
`52`). The substance is sound: both ledger rows are genuinely closed in their owning source docs, the
recorded loader mechanism matches the shipped code (not laundered), REAL-002 was closed from a real
build artifact rather than inference, and the task correctly stayed out of scope. `reviewed` is
satisfied by this document; `merged` remains the owner's.

This was a hybrid `device-verify` reframed (correctly) as **doc-cleanup + an agent-ownable build
check** — REAL-002's close condition is "build verification on the pinned toolchain," not a device
render; REAL-003's render was already device-proven on U3-006. The executing agent honored that
framing and the do-not-launder caution.

## What was verified

- **Closure rule — satisfied.** REAL-002 is recorded in both owning docs (`structure.ios.md` §Render
  paths + `52` Findings); REAL-003 in both (`structure.android.md` §Render paths + `52` Findings). The
  rows are true in their source docs, then flipped in the ledger — not laundered through the ledger.
- **Loader claim truthful to code (the key check).** `52`/`structure.ios.md` state the hosted path
  resolves `ShaderLibrary(url:)` against `FxShaders.bundle` with a `ShaderLibrary.default` fallback.
  `packages/ios/FxShaderView.swift:20-32` does exactly this — builds a search list, appends
  `FxShaders.bundle/default.metallib`, returns `ShaderLibrary(url:)` when present, else `.default`. The
  open "which loader does the hosted path use" question is genuinely resolved, and the doc does not
  overstate the code.
- **REAL-002 closed from a real artifact.** `xcodebuild` produced `FxShaders.bundle` (unmangled) with
  `default.metallib` (194 KB) at the bundle root in the build products — REAL-002's literal condition.
  Not inferred from U1-004's compile-proof or U3-006's render.
- **REAL-003 closed correctly** — path recorded in `structure.android.md` (`src/main/assets/shaders/`,
  `context.assets.open(...)`, API-33 gate), render device-proven on U3-006 (API 35). No redundant run.
- **Scope respected.** U1-001 / IMPL-001 were not closed (correctly out of scope). No new shaders,
  BYO, or compiler work crept in.
- **Docs-only change.** The loader code is pre-existing (U3-006 fix #3); U3-005 only documented it, so
  the reported green headless (tsc/build/lint/swift:lint/test 18/18) is consistent with an unchanged
  build surface. Not re-run by the reviewer — no code delta to re-verify.

## Rules gate (all pass)

- **#1 native owns the frame loop** — the documented mechanism keeps `time`/`resolution` native-injected;
  no per-frame JS introduced.
- **#6 iOS and Android are peers** — both REAL rows closed together; neither platform left trailing.
- **#7 Expo Modules + Fabric** — packaging via the podspec `resource_bundles` and Android assets; no
  custom native boundary, no JSI/C++.

## Findings

| # | Finding | Severity | Status |
|---|---------|----------|--------|
| ① | **Contradictory toolchain strings across source docs.** `52` Findings says "On the pinned SDK 56 toolchain (Xcode 26 / Swift 6.2) … Build-verified on `macos-26` CI"; the ledger REAL-002 note + `notes.md` say the build-verify was a **local** `xcodebuild` on **Xcode 26.5 / Swift 6.3.2**. Two SoT docs disagree on the version *and* on CI-vs-local. Truth (per notes): the build-verify was local on 26.5/6.3.2; U1-004's CI separately compiled on macos-26. | minor (doc consistency) | open |
| ② | **"Pinned toolchain" slightly overclaimed.** REAL-002's condition names "the pinned SDK 56 toolchain" (CI pins Swift 6.2); the actual build was newer (6.3.2). Acceptable evidence — the bundle layout is fixed by the `resource_bundles`/`MTL_LIBRARY_OUTPUT_DIR` mechanism, which is toolchain-stable — but the doc should say "a newer local toolchain," not imply the pinned one. Fold into ①. | minor | open |
| ③ | **IMPL-001 row now stale; U1-001 unblocked.** REAL-002 was IMPL-001's *sole* remaining residual (and U1-001's). The IMPL-001 ledger row (`decision-ledger.md:142`) still lists "the native build/autolink proof (REAL-002, **device**)" as an open residual — both now wrong (resolved; and it's a build check, not device). With REAL-002 done, IMPL-001 is closeable → **U1-001 should move `docs-pending` → `ready-to-merge`**. Correctly *not* U3-005's job to close, but the hand-off + the stale wording should be actioned next. | follow-up (high value) | open |

None block the technical correctness. ①/② are a wording reconciliation; ③ is a downstream hand-off.

## Closure chain — verified

- **REAL-003** — recorded in owning doc `structure.android.md` first, then `52` Findings, then the
  ledger flipped `open → resolved`. Cardinal rule honored.
- **REAL-002** — recorded in `structure.ios.md` + `52` Findings, build-verified, then ledger flipped
  `device-pending → resolved`. The build artifact (not inference) is the evidence.
- **No over-close** — U1-001 / IMPL-001 left open (their closure is a separate task). Correct, though
  see finding ③: they are now *unblocked* and should be picked up.

## Notes for downstream (not U3-005 work)

- **U1-001 is the immediate next win.** REAL-002's closure clears IMPL-001's last residual; U1-001
  becomes `ready-to-merge` once IMPL-001 is closed in its owning docs. Update the IMPL-001 row's stale
  "REAL-002, device" residual as part of that.
- The iOS hosted loader and the bundle mechanism are now documented as the single home in
  `structure.ios.md`; future shader work should not re-derive them in a task.

## Addendum — fix round verification (2026-06-09)

The three findings above were addressed (5 files, all `research/`, green). Re-reviewed:

- **① / ② — resolved.** `52` Findings now reads "Build-verified locally on Xcode 26.5 / Swift 6.3.2
  (U3-005); the CI `bare-ios` job (U1-004) separately confirmed native compile + autolink on
  `macos-26` / Xcode 26. The bundle layout is determined by the podspec mechanism, which is
  toolchain-stable." The conflation and the overclaim are both gone.
- **③ — IMPL-001 closed legitimately (verified not laundered).** Gated against the cardinal rule:
  the close condition ("the package builds and describes itself per `52`/`53`/`51`") is genuinely
  met — builds (U1-004 CI + U3-005 local), describes itself (U1-001 identity/scaffolding), and all
  three consumed rows (SHIP-001, RT-010, REAL-002) resolved in their source docs. Honest close.

### Two residual bookkeeping inconsistencies (introduced by ③, minor — not correctness)

Closing IMPL-001 inside this U3-005 fix session (rather than a dedicated U1-001 pass) left two stale
spots a tracker pass should clear:

1. **U1-001 state is now inconsistent.** Its detail block still says *"docs-closed is **partial**"* and
   *"next session's handoff,"* and the table shows `docs-pending`. But its only `Closes:` rows —
   SHIP-001 + IMPL-001 — are now **both resolved in source**, so by the cardinal rule docs-closed is
   **complete**, not partial. U1-001 should be **`ready-to-merge`**; nothing substantive remains but
   the state bump and the owner's `merged` gate.
2. **U3-005 row carries a stale caveat.** The table proof still reads *"reviewed — approved contingent
   on a `52` toolchain-wording fix (①/②)."* That fix is applied, so the caveat is satisfied — the row
   should read **"reviewed (approved)"** and U3-005 is cleanly `ready-to-merge`.

After that one tracker pass, **U3-005 and U1-001 both sit at `ready-to-merge`** for the owner's
`merged` gate — a clean batch. Both are bookkeeping; the underlying closures are sound.
