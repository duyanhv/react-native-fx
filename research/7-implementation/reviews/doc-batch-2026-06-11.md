# Docs batch DOC-016..020 — review

Date: 2026-06-11
Branch: `integration/0.1.x` · docs-only change set (17 modified files + 5 task folders)
Scope: each task's edits vs its critique finding, the source-doc ownership map, and the
shipped code where claims are grounded. Review-only — the reviewer changed no task content;
one tracker linkage fixed (below).

## Verdict

**All five approved.** No ledger row is falsely closed (the batch closes none; the two
narrowings annotate already-resolved rows in both the index and the reconciliation table).
The two recurring fresh-agent defects were checked for and are absent: no per-platform
mechanics restated outside `structure.android.md` (the spine cites the mechanic home), and
no list-continuation churn on neighboring items. `git diff --check` clean; docs-only, so no
lint gate per the session protocol.

## Per-task verdicts

- **DOC-016 (F19)** — §Contributing as a human added to the Contributing Guide; correctly
  scopes the agent machinery as non-binding for drive-by fixes while routing design-question
  changes through the research path and the ledger. Pass.
- **DOC-017 (F4)** — the spine now teaches the shipped Android hosted mechanic. The key
  structural property holds: `structure.android.md` (the mechanic home) carries the full
  mechanic (plain `View`, `RuntimeShader` through `Paint` in `onDraw`, `Choreographer`
  clock, and the deliberate non-use of `createRuntimeShaderEffect` for generative shaders,
  with the reason); `01`/`02`/`research/README.md` describe and cite, never restate. The
  `02` hosted-rung `applyVia` corrected to `Paint.onDraw` with the Compose-era values
  explicitly marked as the deferred mechanism. The self-contradiction inside
  `structure.android.md` (Compose-host §Substrates vs plain-View elsewhere) is gone. Pass.
- **DOC-018 (F5+F12, maintainer-decided)** — the scope ceiling is documented where it
  belongs (`42` §The scope ceiling) with the honest rule #7 trade-off stated (fx accepts
  the ceiling rather than Reanimated's commit-hook model); the corollary is cross-linked
  from `35`. The presence narrowing (`transient` only; `sheet`/`modal` → MOT-001) is
  consistent across every surface that carried the old set: `42` (catalog, Decision 7,
  open questions, reduce-motion line), `41` (Decision/Open questions + the
  `preset="sheet"` example fixed), `50` Decision 7, `56` (table + Decision 5), ledger
  SURF-003 + MOT-001 (annotated, not re-closed) + the reconciliation row, and the
  `data-layer` catalog note (rows kept as MOT-001 territory). The React-semantics table in
  `35` adopts the *model* from the Reanimated precedent, never the forbidden mechanism,
  and each row is framed as a U7-001 implementation rule + U7-002 device row. Pass.
- **DOC-019 (F8)** — `tune` deferral recorded as `41` Decision 13 with the reasoning (the
  triad is load-bearing; `tune`'s semantics are device-pending via MOT-002 and overlap
  `preset`/`transition`); every V1 surface mention is marked or stripped (`50` table +
  example + Decision 2, `54` examples + decisions, `56`, `42`, `1-surface/README`,
  `architecture` flow, `data-layer` §4) while the design is retained in place for MOT-001's
  resurrection. Ledger MOT-002 stays open and is correctly re-scoped to non-V1-blocking.
  Pass.
- **DOC-020 (F18)** — the canonical table in `40` §Native ↔ public event-name mapping was
  verified against the shipped registrations and matches exactly: every view registers
  `onFxTransitionEnd`/`onFxLoad`/`onFxError` (`FxModule.swift:13,38,60`,
  `FxModule.kt:20,42,64`), `FxSurfaceView` adds `onShaderPress`/`In`/`Out`; the two unwired
  rows (`onStateChange`, `onLongPress`) are honestly marked with their future prefixed
  names. `50` and `architecture.md` now point at the table instead of restating it. The
  no-rename call (a code change, out of scope) is correct. Pass.

## Fixed during review (tracker linkage, not task content)

- `progress.md` U7-001 row: `blocked by` now carries DOC-018 (its row declared "blocks
  U7-001" but the dependency was not recorded on U7-001's side; `35`'s new rows are
  prerequisites the presence FSM must answer).

## Carry-forwards (correctly flagged by the batch, no action now)

- U7-001 must answer the five React-semantics rows in `35` (each becomes an implementation
  rule + a U7-002 device row).
- The U2-003 carry-in (actually dispatching `onFxLoad`/`onFxError`) stays a code task;
  DOC-020 only pins the names.

Remaining gate on all five: `merged` (maintainer).
