# U8-001 · the cooperative press recognizer + SDF hit-test — review

Date: 2026-06-13
Branch: `integration/0.1.x` · gate commits `e2ec3dd` (recognizer + uniforms), `35e15b0`
(iOS pass-through + px→dp), `8894cfe` (Android surface lever), `9497631` (child-occlusion
fix). Device-proven across the original gate + two re-gates, maintainer-ratified 2026-06-13.
Scope: the press FSM, the interactive uniforms, and the `none`-mode pass-through vs the task
contract (`tasks/U8-001/README.md`); evidence at `evidence/device.md` + `device-regate.md`.
The planner thread reviewed and wrote this; the runs and PASS verdicts were the maintainer's.

## Verdict

**Approved.** `FxPressHandler.{swift,kt}` ships behind `interactionMode` passive/active on both
platforms — slop self-failure as the cooperative scroller yield, handler-owned long-press
timers on platform tokens with tap suppression, semantic `onShader*` events in view points,
`pressDepth` smoothing for a rendered cancel spring-back, the iOS composition-pass-through
`hitTest`, and the Android `none`-mode pass-through via `ReactPointerEventsView`. `RT-006`
closes (cost half device-proven; feather half re-homed to `DEF-019`); `REAL-005` resolves
(Option A device-proven, mechanism citation-grounded). `reviewed` is satisfied by this document.

## Headless rounds

Round 1 returned seven findings (three blocking — an iOS `unowned` deinit trap, Android passive
losing the MOVE stream, a vertically mirrored Android touch.y — plus contract items: tap-after-
long-press, snap-not-spring cancel, missing iOS pass-through, per-frame uniform-probe exceptions).
All fixed and verified at file:line in round 2 before the gate. The preflight corrections
(stock-recognizer `isEnabled` force-fail, `cancelsTouchesInView=false` divergence, platform
long-press tokens) shipped as designed.

## The pass-through arc (rounds 3–5) — the load-bearing process note

Row 4 (`none`-mode pass-through) failed both platforms at the original gate. Its resolution is
worth recording because it nearly went the wrong way:

- **Round 3** fixed iOS (the always-present `intermediateContainer` joins the bare-surface hit
  set) and the px→dp event coordinates; the Android-parity `ReactPointerEventsView` attempt hit
  a compile-classpath blocker and was correctly reverted + STOP-flagged.
- **Round 4** shipped the Android surface lever (`ReactPointerEventsView` `BOX_NONE`/`AUTO` via
  `compileOnly react-android` — the library's first direct `com.facebook.react.*` coupling,
  ruled rule-#7-clean as native view-system participation). The re-gate **failed Android 4a**,
  and the runner concluded "Fabric does not consult `getPointerEvents()` on the non-`ReactViewGroup`
  `ExpoView`" — a reasonable inference, but one the sparse RN clone could not verify.
- **The correction.** Rather than accept the inference and pivot to the documented JS-`pointerEvents`
  fallback, the planner read the **running** RN 0.85.3 `TouchTargetHelper.kt` in `example/node_modules`.
  It disproved the inference: `:320` consults `pointerEvents` on **any** `view is ReactPointerEventsView`,
  so the surface's `BOX_NONE` was honored all along. The real cause was child occlusion — the AGSL
  `FxSurfaceShaderView` and the `intermediateContainer` are full-bounds `AUTO` children that `BOX_NONE`
  descends into and returns as `SELF` targets (`:355`→`:216`→`:388`), the same mechanism the gate saw
  as the "4b shader-occlusion confound." The JS fallback would have failed identically — it never
  touches the children.
- **Round 5** marks the two children non-targets (`FxSurfaceShaderView` → `NONE`;
  `intermediateContainer` → `FxPassthroughContainer` `BOX_NONE`), the Android analogue of the iOS
  `hitTest` excluding `metalView` + `intermediateContainer`. The re-re-gate **PASSED both platforms**.

The lesson, kept for the next ambiguous device verdict: **an inference a citation cannot reach is a
hypothesis, not a root cause.** Reading the running source (not the sparse reference clone) settled it
in one file and saved a wasted build-and-gate cycle on a fallback that shared the true bug's blind spot.

## Device evidence reconciliation

- **Press FSM + uniforms (original gate, 5/6 PASS both platforms):** passive continuous tracking with
  zero events; active In/Out/Press ordering; exactly one long-press with tap suppression; slop-yield
  cancels with a rendered spring-back and no `onShaderPress`; rapid-touch cost clean (Android gfxinfo
  1.23%, counters input-accounted) — this is `RT-006`'s cost proof.
- **Pass-through (re-re-gate, PASS both platforms):** Android 4a bare tap reaches the behind-`Pressable`
  (`#1 behind`, no `onShader*`); 4b inside child tappable under a live shader (`#0 inside`); px→dp
  confirmed (`{120,120}` dp, not px); iOS 4a/4b re-confirmed (Android-only round 5, source byte-identical).

## Caveats accepted (disclosed in the evidence)

1. **SDF is full-bounds in V1** — no shipped shader exposes a shape uniform, so the hit-test uses the
   `32` D4 fallback; the feather/threshold tuning is `DEF-019` (trigger: first shaped shader), not part
   of this close.
2. **iOS active no-regression not re-exercised in round 5** — the harness mode chips overlap the iOS
   large-title nav bar (harness-cosmetic); iOS active was device-proven at the prior gate and the round-5
   source is Android-only, so the 4a/4b passes confirm the none-path intact. Disclosed, low risk.
3. **`controlled` mode** collapses to the none path (out of V1 scope) with a `TODO` marker.

## Closure

- **RT-006 → resolved:** cost half device-proven; feather/threshold half re-homed to **DEF-019**.
- **REAL-005 → resolved:** Option A device-proven both platforms; mechanism citation-grounded at
  `TouchTargetHelper.kt:320/:339/:355/:388`; the JS fallback retired; the `compileOnly react-android`
  coupling established as a rule-clean precedent.
- **`40` `onLongPress`** flipped to wired (`onShaderLongPress`); **`32`** cost question resolved, feather
  deferred.
- **U8-002** (the full RNGH/`@gorhom` coexistence matrix, closes RT-001) is now unblocked.
- Lifecycle ticked through `docs-closed`; the `merged` tick is the maintainer's.
