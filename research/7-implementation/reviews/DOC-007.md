# DOC-007 · mesh ergonomics + shader catalog — review

Date: 2026-06-08
Type: `ratify` · Closes: FX-001, FX-004
Scope: decision true in the **owning source docs** (`20`/`22`/`50`), with a reality-check against code.

## Verdict

**Approved.** A clean, well-scoped ratification that passes the check ratify tasks usually fail — the
decision is recorded in the owning docs, not just asserted in the ledger, and the "5 implemented" claim
matches the actual catalog + Metal source.

## Verified

- **Cardinal rule — true in owning docs:** `20-fills.md` pins the full `width × height` mesh grid (N-stop
  sugar out) and mesh-only `drift`; `22-shaders.md` records the 10-id catalog and the 5-implemented /
  5-pending split. FX-001 and FX-004 are `resolved` with reconciliation rows back to the propagated docs.
- **Reality-check:** `catalog.ts` exposes exactly the 5 implemented ids (`fractal-clouds`, `ink-smoke`,
  `liquid-chrome`, `loop`, `dots`); `FxShaders.metal` implements exactly those 5 fragment functions. The
  5 unimplemented ids are **not** in the TS type — consumers can't reference them. Scope guard ("don't
  treat unimplemented ids as runtime-ready") honored.
- **Gap tracked:** U3-006 owns the native MSL+AGSL impl for the remaining 5 ids + the unified `ShaderId`.
- **Rules gate:** agnostic ids (#2); curated catalog, BYO held to FX-006/U3-004 (#5); public uniform =
  `intensity` only, `time`/`resolution`/`pressDepth`/`touch` native-owned (#1, the contract).

## Watch-items (downstream, not defects)

1. "V1 catalog (10)" ≠ "V1 shipped (5)" — the 10-id claim is contingent on U3-006 landing in V1; the TS
   type correctly guards it. Keep the distinction loud.
2. `edge-glow` shader id vs DOC-004 (ship effect components?) — keep consistent when DOC-004 lands.
3. `loop` / `dots` are weak public names, but DOC-007 inherited them from `catalog.ts` (U1-001) — out of
   scope here; future-naming watch-item.
