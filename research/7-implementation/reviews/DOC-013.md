# DOC-013 · curated shader authoring pipeline — review

Date: 2026-06-08
Type: `ratify` · Closes: REAL-004
Scope: decision true in the owning doc (`52`, with `03`), reality-checked against the existing shaders
and the compiler deferral.

## Verdict

**Approved.** A correctly-scoped ratify: V1 curated shaders are hand-maintained MSL+AGSL pairs; the
author-once compiler stays additive V2. The decision is recorded in `52`, the ledger row closes against
it, and the compiler track was left untouched.

## Verified

- **Cardinal rule — true in owning doc:** `52` item 9 records "V1 curated shaders are hand-maintained
  MSL+AGSL pairs… author-once compiler remains additive V2." REAL-004 is `resolved` with a matching
  reconciliation note.
- **Compiler track intact:** `03` (adapter v1 / compiler v2) unchanged; **SPINE-008 remains `deferred`**.
  DOC-013 did not close it — exactly right; the authoring decision and the compiler IR are different
  questions.
- **Consistent with reality:** the existing five shaders in `FxShaders.metal` are hand-written MSL, so
  "hand-maintained pairs" matches what's already shipped; U3-006 just adds the AGSL halves by hand.
- **Sequencing fixed:** U3-006 now consumes REAL-004 and is blocked by DOC-013 — the rework risk that
  motivated doing DOC-013 first is closed.
- **Scope guard honored:** no shader code, no transpiler/codegen, no SPINE-008 / REAL-002 / REAL-003
  closure.

## Watch-item (for U3-006, not a DOC-013 defect)

DOC-013 now mandates an **MSL+AGSL pair for every curated id**, but only the five *new* ids
(`aurora`/`noise-field`/`plasma`/`caustics`/`edge-glow`) are in U3-006's stated scope. The original five
(`fractal-clouds`/`ink-smoke`/`liquid-chrome`/`loop`/`dots`) have MSL but **no AGSL yet** — so Android
shader support for them is unaccounted for. When U3-006 is spec'd, confirm its scope covers AGSL for all
ten curated ids (or explicitly carve the original five into their own Android-AGSL task), so Android
shader support isn't left half-done.

## Note

`52` flags a future optional `@react-native-fx/compiler` package split as "worth considering" — a
forward note tied to SPINE-008 / SPINE-006, not a V1 commitment. Fine as written; it does not pre-decide
the compiler.
