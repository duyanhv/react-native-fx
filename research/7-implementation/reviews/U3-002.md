# U3-002 · hosting parity / glass styles / uniforms — review

Date: 2026-06-10
Branch: `integration/0.1.x`
Scope: the UIKit glass-rung rework (code + ratified docs) against the spike
(`research/wip/interactive-glass-touch-delivery.md`), the work order
(`tasks/U3-002/README.md`), the platform contract (`structure.ios.md` §material), and the
device evidence (`tasks/U3-002/evidence/device-run-2026-06-10/`). The reviewer made one
trivial code fix (noted below); everything else is review-only.

## Verdict

**Approved.** The rework is a faithful realization swap — capability model unchanged
(`interaction:'self'`, `hosted`, the `MaterialConfig` bridge contract untouched), rules
#2/#3/#4 intact — and it resolves A2-1, A2-2, and A2-4 together, which the spike proved the
SwiftUI rung could not do. Devices confirm it: all A2/A4 scenarios pass, the human gate
(maintainer live tap) confirms the visible press. `device-verified`, `reviewed`, and
`docs-closed` are satisfied; `merged` remains the owner's.

## What was verified

- **The rework matches the pinned mechanic.** `FxGlassSurfaceView` mirrors the
  `expo-glass-effect` precedent's three lifecycle quirks exactly (effect created in
  `layoutSubviews()`, stale-effect clear before `isInteractive` reassignment,
  `setNeedsLayout()` on window re-entry) — and the mechanic was pinned in
  `structure.ios.md` before the code landed, per the operating rule. ADOPT/REJECT cells
  honored: no tint/colorScheme/per-corner radii, no `NSClassFromString` beta probe, no RN
  child hosting (rule #4).
- **Mount-path hygiene.** The UIKit and SwiftUI mount paths in `FxHostedView` are exclusive
  and tear each other down on effect switches; radius changes update `cornerConfiguration`
  in place without remounting; the leftover diagnostic `NSLog` is gone. Below 26 the
  intensity fallback hosts unchanged.
- **Closure rule — satisfied.** FX-002 closed in `21` (Open questions) + ledger; FX-005 in
  `22` + ledger (Metal via sweep A3, AGSL via the B2 Android shader renders); SPINE-012 in
  `01` + ledger (B2 Android parity, A4 many-boundaries, and the coexistence answer). True in
  the owning docs first, then flipped in the ledger.
- **The press proof is backdrop-independent.** No frame-diff anywhere. The signal is
  structural: `isInteractive=true` installs `_UIFlexInteraction` +
  `_UIUpdateLinkViewInteraction` on the effect view (absent when static), captured from a
  genuine `log stream`; the visible response was confirmed by the maintainer's live tap.
- **Headless gates.** Biome, swift-format, build, 21/21 tests, both `tsc` runs, and
  `xcodebuild` BUILD SUCCEEDED (after the required `pod install` — the podspec glob
  resolves at install time).

## Findings

1. **(Fixed in review)** `FxMaterialView.swift` kept a dead `ExpoModulesCore` import after
   `MaterialConfig` moved out; dropped to match the pure-SwiftUI idiom (`FxFillView`).
   Lints re-run clean.
2. **(Doc finding, resolved)** A4-10 contradicted the spike: the UIKit rung is
   scroll-through (a drag beginning on the interactive glass pans the parent list), where
   the SwiftUI rung captured such drags. `01` decision 6, `structure.ios.md` §material, and
   the scenario step 10 were amended to record the arbitration as rung-specific.
3. **(Dispositioned, non-blocking)** The below-26 fallback renders sharp corners on iOS
   18.5. Pre-existing — the below-26 branch was a plain `Rectangle().fill(material)` before
   the rework too. Logged in `notes.md` as a polish candidate (clip to the host layer
   radius), not a V1 gate.
4. **(Watch item)** The interactive-glass stage read ~40 fps on the iOS simulator during
   the original sweep; recorded in the SPINE-012 closure. Watch on hardware.
5. **(Process note)** Before this task's `pod install`, the stale Pods file list was also
   missing `FxSymbolView.swift` — prior "BUILD SUCCEEDED" evidence may have compiled an
   older Pods state than the tree it claimed to prove. On-device results stand; future
   build-evidence claims should confirm the file list is current.
