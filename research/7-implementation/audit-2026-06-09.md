# Implementation-layer consistency audit — 2026-06-09

Read-first audit of `research/7-implementation/` cross-checked against the research planes (0–6)
and the real code in `packages/` (src + ios + android). Every finding is grounded in `file:line`.

**Headline: the integrity contract holds.** The cardinal closure rule passes clean — no laundering.
Every `Closes:` ledger row on a merged task is genuinely recorded in its owning source doc (full
sweep in [Cardinal-rule sweep](#cardinal-rule-sweep)). Shipped code carries **zero** forbidden
internal-artifact comment refs. No orphaned `tasks/` folders; no stray repo-root `tasks/`. The
findings below are doc-vs-code drift, a reviews-coverage gap, and bookkeeping staleness — none
block the build.

## Method

- Cardinal-rule + ledger sweep: done in-session against `decision-ledger.md` and the plane docs.
- Four read-only subagents parallelized: arch/data-layer-vs-code drift, tasks-folder hygiene,
  forbidden-comment sweep, and a source-doc closure verifier for all 10 merged tasks' `Closes:` rows.
- One inline fix applied (logged under [Fixed inline](#fixed-inline-unambiguous-bookkeeping)).

---

## Fixed inline (unambiguous bookkeeping)

- **Broken evidence link** — `progress.md:123` referenced `tasks/U4-001/evidence/device.md`
  (bare-id form); the real folder is the slug form `tasks/U4-001-wrapper-mechanic/`. The file
  exists at `tasks/U4-001-wrapper-mechanic/evidence/device.md`. Corrected the path. (Single
  occurrence — `grep` confirmed no other bare-id `tasks/U4-001/` reference.)
- **N2 — stale local `task.md` copies swept** (post-audit, on maintainer instruction). Synced the
  three superseded local `task.md` files to their authoritative `progress.md` detail blocks:
  `U2-002` (`state: in-progress`→`merged`, "resume here" banner removed, lifecycle boxes checked);
  `U4-001` (`State: device-pending`→`merged`, device-proof box checked, `merged` box added);
  `U4-002` (`State: headless-done`→`merged`, the four trailing boxes checked, and the stale
  "iOS diagnostic logging added — superview guard deferred" sub-bullet corrected to the merged
  reality — symmetric mount/unmount + superview guard, diagnostics removed).
- **N1 — RT-010 Reconciliation verb flipped** (post-audit). `decision-ledger.md:171` action
  `propagate`→`resolved`; the half-true "already reflects several views" line was rewritten to name
  the three real views and point the §1 phantom cleanup at DOC-014.

The substantive items (S1–S3) remain flagged — they are the maintainer's call.

---

## Blocking

**None.** No laundered closure, no merged task with an open `Closes:` row, no rule breach in shipped
code.

---

## Should-fix

### S1 · Doc-vs-code drift exceeds DOC-014's stated scope
> **Dispositioned (2026-06-09):** APPROVED — DOC-014 row widened to the full ref set + the pointer fix; nuance: `FxManagedView` = true phantom (drop); `FxPresenceView`/`FxPressableView` = planned V2 bindings (mark planned, don't delete). Executed by DOC-014 in the finish-V1 session.

DOC-014 (todo) already owns reconciling the `FxPresenceView`/`FxManagedView`/`FxPressableView`
phantoms. Confirmed, and the real registered views are exactly three — `FxHostedView`,
`FxSurfaceView`, `FxGroupView` (`packages/ios/FxModule.swift:12,31,53`;
`packages/android/.../FxModule.kt:19,38,60`; JS at `packages/src/runtime/FxHostedView.tsx:17`,
`FxSurfaceView.tsx:34`, `FxGroupView.tsx:15`). But the phantom refs leak **outside** DOC-014's
written scope (`architecture.md §1` + `data-layer.md §5.1`), so the task as scoped won't catch them:

- `architecture.md:59-61` — the `src/runtime/` block lists the 3 real bindings **and** the 3
  phantoms side by side. (In DOC-014 scope.)
- `architecture.md:323-324` — Path-2 content-motion flow routes through
  `src/runtime/FxPresenceView.tsx`; the real binding is `FxSurfaceView.tsx`. (Outside scope.)
- `architecture.md:556-557` — Unit-7 JS file = `FxPresenceView.tsx`, Unit-8 = `FxPressableView.tsx`;
  neither exists. (Outside scope.)
- `architecture.md:77` — podspec named `react-native-fx.podspec`; real file is
  `ReactNativeFx.podspec` (the inline `[IMPL-001: post-identity-pass name]` annotation predicts the
  rename but the line still shows the old name). (Outside scope.)
- `data-layer.md:724-726` — entity diagram lists the same 3 phantoms (`FxManagedView` never existed
  in any form). This is the **§9 entity diagram, not §5.1** — so DOC-014's pointer "`data-layer.md
  §5.1` runtime src-trees" is mis-cited (`§5.1` at `data-layer.md:642-647` is memoization guidance).
- `data-layer.md:693-695` — claims `src/manifest/CapabilityManifest.ts (data)` ships; the only real
  manifest files are `index.ts` + `select.ts` + `types.ts` (`packages/src/manifest/`). No
  `CapabilityManifest.ts`; the executable manifest *data* is not in `src/` at all — it lives only in
  the test fixture `packages/src/__tests__/manifest-select.test.ts`. (Outside scope.)

**Recommended fix:** widen DOC-014 to cover all phantom/stale refs (the §2 path-flow at 323-324, the
Unit-7/8 refs at 556-557, the podspec name at 77, the `data-layer.md` §9 entity diagram at 724-726
and the manifest-data claim at 693-695), and correct DOC-014's own section pointer from
`data-layer.md §5.1` to the §9 entity-diagram region.

**Note (not drift):** `architecture.md §1` is explicitly a "Target Folder Structure" (forward-looking
tree), so the not-yet-built natives it lists — `FxLayoutObserver`/`FxAnimationDriver`/
`FxPresenceCoordinator`/`FxPressHandler` on both platforms (`architecture.md:71-74,85-88`) — are
intended Unit 5–9 slots, **not** drift. They should be annotated "pending (Unit 5–9)" so they aren't
mistaken for shipped. Conversely, three **real** internal render structs are undocumented:
`FxShaderView.swift:13`, `FxFillView.swift:7`, `FxMaterialView.swift:9` (iOS SwiftUI helpers, not
Fabric views) and `FxShaderView.kt:27` (Android). Worth a one-line "hosted render structs"
sub-listing so they aren't confused with registered views.

### S2 · Reviews-coverage gap on three merged tasks
> **Dispositioned (2026-06-09):** RESOLVED — recorded in `reviews/merge-batch-2026-06-09.md` (U3-006, U4-001, U4-002 verdicts + the U4 reference-fan-out provenance).

The pattern is: substantial implement/device tasks get a `reviews/<id>.md` (U3-001, U3-005, DOC-007/
008/013, U1-001), and the V1 housekeeping sweep is covered by
`reviews/v1-merge-batch-2026-06-08.md` (U1-002/003/004/005, U2-001/002, DOC-001/007/008). Three
merged tasks fall through both nets:

- **U3-006** (`progress.md:59`) — merged, device-verified, a 10-shader implement task. Says
  "reviewed + confirmed by maintainer (2026-06-09)" but has **no `[review]` link**, no
  `reviews/U3-006.md`, and is **not** in the 2026-06-08 batch (which predates its 06-09 review). Its
  Unit-3 peers U3-001 and U3-005 both have standalone review docs.
- **U4-001** (`progress.md:114-127`) — the detail-block checklist has **no `reviewed` box at all**;
  it goes `ledger RT-015 closed → merged`, skipping the `reviewed` lifecycle gate on paper.
- **U4-002** (`progress.md:148`) — checklist shows `[x] reviewed` ("diffed against templates") but no
  review artifact; merged 06-09, after the batch.

The cardinal sweep confirms all three have their `Closes:` rows genuinely resolved in source, so this
is a **process-record gap, not a correctness defect** — the maintainer evidently reviewed inline.

**Recommended fix:** record the `reviewed` gate for the three — either a second batch doc
(`reviews/v1-merge-batch-2-2026-06-09.md` covering U3-006 + U4-001 + U4-002) or per-task
"reviewed (no separate doc)" notes mirroring DOC-002/005/010 — and add the missing `reviewed`
checklist box to U4-001's detail block.

### S3 · SURF-010 closes against a plane-7 doc, not its named plane-1 source
> **Dispositioned (2026-06-09):** APPROVED — lodged as DOC-015 (doc-cleanup): propagate the memo guidance into the plane-1 source (`50`/`54`/`55`) and re-point the SURF-010 closure, or correct the attribution. Executed in the finish-V1 session.

`SURF-010` is `resolved` (`decision-ledger.md:68`) with source named as `50`/`54`/`55` (plane 1). But
the memoization decision it closes ("native `previousProps` value-equality → no manual memo") is
recorded in `data-layer.md §5.1` (`data-layer.md:642-647`) — a **plane-7** materialization doc. The
ledger's own rule (`decision-ledger.md:15-17,146`) is explicit that plane-7 provisioning is *not*
closure and must propagate back to the source plane. Correspondingly, the Reconciliation row's action
verb still reads **`propagate`** (`decision-ledger.md:162`), never flipped to `resolved`, even though
the main row is `resolved`. The close is **defensible** (the cardinal-sweep confirmed `41`/`55` are
clean — there's no contrary plane-1 statement to fix), but it is technically closed against plane 7.

**Recommended fix:** either drop a one-line memoization-guidance memo into `55` (or `54`) and flip the
Reconciliation action to `resolved`, or re-point SURF-010's `source` column to `data-layer §5.1` and
update the action verb so the index stops reading mid-propagation.

---

## Nits

### N1 · RT-010 Reconciliation action verb stale; "reflects several views" half-true — FIXED

`RT-010` is `resolved` (`decision-ledger.md:113`), but its Reconciliation action read `propagate`
(`decision-ledger.md:171`) and asserted "`architecture.md` already reflects several views" — only
half-true, since the same §1 block (`:59-61`) also lists the 3 phantoms. RT-010's substantive close
(registration without duplicate-registration errors) is genuinely done; the residual was cosmetic.
**Fixed:** action flipped to `resolved`; the line now names the three real views and attributes the
§1 phantom cleanup to DOC-014.

### N2 · Stale local `task.md` copies contradict merged state — FIXED

Per `progress.md:450` ("the table is the view; the detail block / folder is the store… on
disagreement the store wins"), the authoritative store is the `progress.md` detail block. Three
folders keep an *older* local `task.md` that was never swept on merge:

- `tasks/U2-002-uniformspec-reconciliation/task.md` — still `state: in-progress` with a live "Next
  action (resume here)" banner and every lifecycle box after `spec'd` unchecked; row is `merged`
  (`progress.md:53`). Strongest contradiction.
- `tasks/U4-001-wrapper-mechanic/task.md` — still `State: device-pending`, device-proof box
  unchecked; row is `merged` (`progress.md:66`).
- `tasks/U4-002-mountChildComponentView/task.md` — still `State: headless-done` with
  `device-verified`/`docs-closed`/`reviewed`/`merged` unchecked; row is `merged` (`progress.md:67`).

Three folders kept an older local `task.md`: `U2-002` (still `state: in-progress` with a live
"resume here" banner), `U4-001` (still `State: device-pending`), `U4-002` (still
`State: headless-done`, four boxes unchecked). **Fixed** on maintainer instruction — all three swept
to `merged` against their authoritative `progress.md` detail blocks (see [Fixed inline](#fixed-inline-unambiguous-bookkeeping)).

### N3 · `tasks/` folder naming is inconsistent

Folders mix slug form (`U1-001-package-scaffolding`, `U4-001-wrapper-mechanic`) and bare-id form
(`U3-005`, `U3-006`, `DOC-007`). Harmless but it's exactly what produced the broken link fixed above
(F-inline). Consider standardizing on one form. Flag-only.

### N4 · U3-006 device-scenario file mislabeled `headless.md`

`progress.md:254` cites the U3-006 **device** proof at `tasks/U3-006/evidence/headless.md`; the file
is titled "U3-006 — Device Scenario". Correct by content, misleading by filename. Optionally rename to
`evidence/device.md` or fold in.

---

## Checks that passed clean (no findings)

- **Cardinal-rule sweep** — all 10 merged tasks' `Closes:` rows verified true in their owning source
  docs. See the table below.
- **Forbidden code-comment refs** — zero violations across `packages/src`, `packages/ios`,
  `packages/android`. The 6 `// TODO:` markers are all compliant (plain capability terms, no internal
  ids). Comments are substantial and present, so this is a true clean, not an empty-file false pass.
- **Blueprint vs reality** — Unit 4 (`blueprint.md:50-55`) correctly describes the *corrected* wrapper
  mechanic: an `fx-original` intermediate, layout-transparent container inside `FxSurfaceView` with
  `mountChildComponentView` routing children into it, animator targeting the container. Matches the
  RT-014/RT-015 closures and the shipped code. No stale unit descriptions found.
- **Tasks hygiene** — no orphaned folders, no stray repo-root `tasks/`, all `[task]`/`[review]`/
  `[detail]` links resolve (after the F-inline fix), every device task has `evidence/`.

### Cardinal-rule sweep

| Task → Closes | Recorded in source doc | Verdict |
|---|---|---|
| DOC-002 → SPINE-004 / SPINE-007 | `0-spine/02:397-401` (Dec 12), `02:402-406` (Dec 13) | TRUE |
| DOC-005 → SURF-003/004/005 | `1-surface/50:150`, `56:87-88`, `57:86,88`, `41:197-198` | TRUE |
| DOC-007 → FX-001 / FX-004 | `2-effects/20:93-96`, `22:131-132`, `50:121-125` | TRUE |
| DOC-008 → FX-009 | `2-effects/24:43-46,98-104` | TRUE |
| DOC-010 → MOT-010 | `41:184-187` (Dec 9), `42:116-125`, `34:86-90` | TRUE |
| DOC-013 → REAL-004 | `6-ship/52:139-141` (Dec 9), `0-spine/03:56-82` | TRUE |
| U3-005 → REAL-002 / REAL-003 | `structure.ios.md:58-63`, `structure.android.md:84-86`, `52:150-161` | TRUE |
| U4-001 → RT-015 | `4-runtime/33:54-62`, `34:70-75` | TRUE |
| U4-002 → RT-014 | `33:41-42,60-69`, `structure.ios.md:145-156`, `structure.android.md:163-186` | TRUE |
| U1-003 → RT-004 / RT-010 / RT-011 / SURF-010 | `31:62-65,112-114`, `51:84-87`, `data-layer.md:642-647` (SURF-010 — see S3) | TRUE* |

\* SURF-010 closes in plane-7 `data-layer §5.1` rather than its named plane-1 source — see S3.

---

## Recommended next actions

1. **Expand DOC-014** to the full phantom/stale ref set (S1) and fix its mis-cited section pointer.
2. **Record the `reviewed` gate** for U3-006 / U4-001 / U4-002 (S2).
3. **Resolve the SURF-010 plane-7 closure** (S3) — propagate a memo or re-point the source.

Bookkeeping (F-inline, N1, N2) is done. The remaining substantive items (S1–S3) are the maintainer's
call.
